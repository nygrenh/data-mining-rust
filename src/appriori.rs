use student::Student;

pub fn sequental(students: Box<Vec<Student>>,
                 sequence_length: u8,
                 top_considered: u8,
                 support: f32) {
    println!("This is sequental!");
}
